spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/fitness-tut-user1
    username: springstudent
    password: springstudent

  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8081

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/